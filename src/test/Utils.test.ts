import { Utils } from '../app/Utils';


describe('Utils test suite', () => {
    it('should return a message on the console', () => {
        const msg = Utils.toUpperCase('ssw');
        console.log('Itsa me a test')
    })
});